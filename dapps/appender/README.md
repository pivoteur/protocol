# `appender`

`appender` takes the output of `wyrd` and writes the close-pivot line to the
close-pivot table (the inode is specified as the final line of `wyrd`-output)

## usage

`$ wyrd <arguments> | appender`

* [src](../../quizzes/src/quiz08/c_appender/mod.rs)

