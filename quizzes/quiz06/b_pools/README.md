# `b_pool`

Reads the pivot pools from the `pools/`-directory in git then partitions pools
by TVL.

![Snapshot of protocol's Pivot pools](imgs/01-assets.png)

> sidenote: this quiz introduces [aliases](../../../libs/src/types/aliases.rs).

