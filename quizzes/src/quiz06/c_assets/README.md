# c_assets

This iteration of `assets`, instead of using pool TVLs (as before), now uses
the totals tallied in the wallet addresses, ... and today's quotes, ... to 
compute the set of assets the protocol holds.

* [src](mod.rs)

