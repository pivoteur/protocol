
// pub mod a_rwr;

pub mod functional_tests {

   use book::err_utils::ErrStr;

   pub async fn runoff() -> ErrStr<usize> {
      println!("\nquiz11 functional tests\n");
      // let n1 = a()?;
      // let n2 = b().await?;
      Ok(0)
   }
}
